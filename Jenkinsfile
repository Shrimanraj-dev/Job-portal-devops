pipeline {
    agent any

    stages {

        stage('Clone Repository') {
            steps {
                git 'https://github.com/Shrimanraj-dev/Job-portal-devops.git'
            }
        }

        stage('List') {
            steps {
                sh 'ls'
            }
        }
		
		 stage('Shri') {
            steps {
                sh 'echo SHRIMAN'
            }
        }

       
    }
}